query MyQuery($year: ID = "2026") {
  Conference {
    RegistrationManager {
      get(key: $year) {
        closeDate
        maxVisitors
        openDate
        registeredVisitors
        year
      }
    }
  }
}
